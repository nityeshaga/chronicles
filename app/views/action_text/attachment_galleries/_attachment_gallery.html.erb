<%# Two or more uploads in a row. The same figure the importer wrote for Ghost galleries
    (lib/ghost/mobiledoc.rb card_gallery), so the gallery CSS serves both. %>
<figure class="kg-card kg-gallery-card kg-width-wide"><div class="kg-gallery-container"><%= yield %></div></figure>
